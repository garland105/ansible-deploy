# ============================================================
# common.yml - 系统初始化
# 作用：所有机器的通用配置（apt源、时区、SSH、防火墙等）
# ============================================================
- name: "系统初始化"
  hosts: all_monitored
  gather_facts: yes
  roles:
    - common
