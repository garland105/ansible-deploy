# handlers file for common role
# Ubuntu 24.04 上 ssh 服务名为 ssh（非 sshd）
---
- name: restart sshd
  systemd:
    name: "{{ 'ssh' if ansible_facts['os_family'] == 'Debian' else 'sshd' }}"
    state: restarted
    daemon_reload: yes
